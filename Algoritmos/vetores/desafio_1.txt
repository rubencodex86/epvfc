Algoritmo

Inicio

var nomes[7] :string;
    contador :inteiro;

    Para contador de 0 ate 6 faça
        Escrever("Insira o nome do aluno: ");
        Ler(nomes[contador]);
    FimPara

    Para contador de 6 ate 0 passo -1 faça
        Escrever(nomes[contador]);
    FimPara

Fim
